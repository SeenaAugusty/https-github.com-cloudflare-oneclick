{
  "name": "cf-oneclick-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "format": "echo 'add prettier if needed'",
    "lint": "echo 'add eslint if needed'"
  }
}